var hostResourceService = {
    hostname: 'http://localhost',
    port : '3001',
};

hostResourceService.URL = hostResourceService.hostname +':'+ hostResourceService.port

module.exports = hostResourceService;